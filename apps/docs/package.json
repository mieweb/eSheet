{
  "name": "esheet-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prebuild": "pnpm generate-doc-content",
    "prestart": "pnpm generate-doc-content",
    "prepreview": "pnpm build",
    "docusaurus": "docusaurus",
    "start": "docusaurus start --host 0.0.0.0 --port 3000 --no-open",
    "build": "docusaurus build",
    "dev": "pnpm start",
    "generate-doc-content": "node scripts/generate-doc-content.mjs",
    "lint": "eslint .",
    "preview": "docusaurus serve --host 0.0.0.0 --port 3000",
    "serve": "docusaurus serve --host 0.0.0.0 --port 3000",
    "clear": "docusaurus clear",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.2",
    "@docusaurus/faster": "^3.10.2",
    "@docusaurus/preset-classic": "3.10.2",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "dotenv": "^17.4.2",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.2",
    "@docusaurus/types": "3.10.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
