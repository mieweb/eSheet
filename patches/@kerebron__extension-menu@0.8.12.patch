diff --git a/esm/CustomMenuPlugin.js b/esm/CustomMenuPlugin.js
index d630c6893dd77f148ca72cf2b137364bbce3830c..5b80fc9ed9dc64ce76b517f9fa6c02df44272f46 100644
--- a/esm/CustomMenuPlugin.js
+++ b/esm/CustomMenuPlugin.js
@@ -622,7 +622,7 @@ export class CustomMenuView {
                         const mousedownEvent = new MouseEvent('mousedown', {
                             bubbles: true,
                             cancelable: true,
-                            view: dntShim.dntGlobalThis,
+                            view: this.editorView.dom.ownerDocument.defaultView,
                         });
                         dom.dispatchEvent(mousedownEvent);
                     }
@@ -759,7 +759,7 @@ export class CustomMenuView {
                             const mousedownEvent = new MouseEvent('mousedown', {
                                 bubbles: true,
                                 cancelable: true,
-                                view: dntShim.dntGlobalThis,
+                                view: this.editorView.dom.ownerDocument.defaultView,
                             });
                             dom.dispatchEvent(mousedownEvent);
                         }
@@ -771,7 +771,7 @@ export class CustomMenuView {
                             const mousedownEvent = new MouseEvent('mousedown', {
                                 bubbles: true,
                                 cancelable: true,
-                                view: dntShim.dntGlobalThis,
+                                view: this.editorView.dom.ownerDocument.defaultView,
                             });
                             dom.dispatchEvent(mousedownEvent);
                         }
@@ -905,16 +905,18 @@ export class CustomMenuView {
             wrapper.setAttribute('draggable', 'false');
             const isDropdown = tool.element.content !== undefined;
             if (isDropdown) {
-                const { dom } = tool.element.render(this.editorView);
+                const { dom, update } = tool.element.render(this.editorView);
                 const dropdownMenu = dom.querySelector('.kb-dropdown__content');
                 if (dropdownMenu) {
                     dropdownMenu.style.display = 'none';
                 }
                 wrapper.appendChild(dom);
+                update(this.editorView.state);
             }
             else {
-                const { dom } = tool.element.render(this.editorView);
+                const { dom, update } = tool.element.render(this.editorView);
                 wrapper.appendChild(dom);
+                update(this.editorView.state);
             }
             // Temporarily add to toolbar to measure
             this.toolbar.appendChild(wrapper);
@@ -1514,11 +1516,7 @@ export class CustomMenuView {
         }
     }
     update(view, prevState) {
-        // Re-render tools to update their state (original approach)
-        // Note: This is less efficient but more reliable than storing update functions
-        this.tools.forEach((tool) => {
-            tool.element.render(this.editorView);
-        });
+        this.render();
     }
     destroy() {
         // Clean up event listeners
diff --git a/esm/buildMenu.js b/esm/buildMenu.js
index 85b1f4762033ee4dc68d1c9ceaa69dbd55f746de..9abcc3ba868130cd89c6653b942a6c5d4c699af9 100644
--- a/esm/buildMenu.js
+++ b/esm/buildMenu.js
@@ -220,6 +220,7 @@ export function buildMenu(editor, schema) {
             title: 'Toggle bold',
             run: () => editor.chain().toggleStrong().run(),
             enable: (state) => editor.can().toggleStrong().run(),
+            active: (state) => markActive(state, schema.marks.strong),
             icon: icons.strong,
         }));
     }
@@ -229,6 +230,7 @@ export function buildMenu(editor, schema) {
             title: 'Toggle italic',
             run: () => editor.chain().toggleItalic().run(),
             enable: (state) => editor.can().toggleItalic().run(),
+            active: (state) => markActive(state, schema.marks.em),
             icon: icons.em,
         }));
     }
@@ -252,6 +254,7 @@ export function buildMenu(editor, schema) {
             title: 'Toggle underline',
             run: () => editor.chain().toggleUnderline().run(),
             enable: (state) => editor.can().toggleUnderline().run(),
+            active: (state) => markActive(state, schema.marks.underline),
             icon: icons.underline,
         }));
     }
@@ -331,24 +334,32 @@ export function buildMenu(editor, schema) {
             icon: icons.alignLeft,
             run: () => editor.chain().setTextAlignLeft().run(),
             enable: () => editor.can().setTextAlignLeft().run(),
+            active: (state) =>
+                (state.selection.$from.parent.attrs.textAlign ?? 'left') === 'left',
         }));
         alignmentGroup.push(new MenuItem({
             title: 'Align center',
             icon: icons.alignCenter,
             run: () => editor.chain().setTextAlignCenter().run(),
             enable: () => editor.can().setTextAlignCenter().run(),
+            active: (state) =>
+                state.selection.$from.parent.attrs.textAlign === 'center',
         }));
         alignmentGroup.push(new MenuItem({
             title: 'Align right',
             icon: icons.alignRight,
             run: () => editor.chain().setTextAlignRight().run(),
             enable: () => editor.can().setTextAlignRight().run(),
+            active: (state) =>
+                state.selection.$from.parent.attrs.textAlign === 'right',
         }));
         alignmentGroup.push(new MenuItem({
             title: 'Justify',
             icon: icons.alignJustify,
             run: () => editor.chain().setTextAlignJustify().run(),
             enable: () => editor.can().setTextAlignJustify().run(),
+            active: (state) =>
+                state.selection.$from.parent.attrs.textAlign === 'justify',
         }));
     }
     // Group 6: Insert tools (Image, Lists, HR, Table, Type)
