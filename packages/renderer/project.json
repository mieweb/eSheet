{
  "name": "@esheet/renderer",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build:css": {
      "command": "npx @tailwindcss/cli -i packages/renderer/src/index.css -o packages/renderer/src/index.output.css",
      "options": {
        "cwd": "{workspaceRoot}"
      }
    },
    "build:css:watch": {
      "command": "npx @tailwindcss/cli -i packages/renderer/src/index.css -o packages/renderer/src/index.output.css --watch",
      "continuous": true,
      "options": {
        "cwd": "{workspaceRoot}"
      }
    },
    "build": {
      "command": "vite build",
      "options": {
        "cwd": "packages/renderer"
      },
      "dependsOn": ["build:css", "^build"]
    }
  }
}
